services:
  - type: web
    name: flower-care-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn flower_server:app -w 2 -b 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
    plan: free
